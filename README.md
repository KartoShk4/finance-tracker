# Finance Tracker

Современное веб-приложение для отслеживания личных финансов с использованием Angular и Supabase.

## 📋 Описание

Finance Tracker позволяет пользователям:
- Создавать категории доходов и расходов
- Добавлять транзакции к каждой категории
- Просматривать графики транзакций с группировкой по дням, неделям, месяцам и годам
- Отслеживать общую сумму по каждой категории

## 🚀 Технологии

- **Angular 21** - фреймворк для построения пользовательского интерфейса
- **Supabase** - Backend-as-a-Service для хранения данных
- **Chart.js** - библиотека для визуализации данных
- **TypeScript** - типизированный JavaScript
- **SCSS** - препроцессор CSS

## 📁 Структура проекта

```
src/
├── app/
│   ├── core/                    # Ядро приложения
│   │   ├── repository/          # Репозитории для работы с данными
│   │   ├── supabase/            # Сервис Supabase
│   │   └── storage/             # Локальное хранилище (не используется)
│   ├── features/                 # Функциональные модули
│   │   ├── items/               # Модели и store для категорий
│   │   └── transactions/        # Модели и store для транзакций
│   ├── pages/                   # Страницы приложения
│   │   ├── home/                # Главная страница
│   │   └── item-details/        # Страница деталей категории
│   ├── shared/                  # Общие компоненты и утилиты
│   │   ├── components/          # Переиспользуемые компоненты
│   │   │   └── chart/           # Компонент графика
│   │   └── utils/               # Утилиты
│   │       ├── chart.utils.ts   # Утилиты для работы с графиками
│   │       └── date.utils.ts     # Утилиты для работы с датами
│   ├── app.ts                   # Корневой компонент
│   ├── app.routes.ts            # Маршруты приложения
│   └── app.config.ts           # Конфигурация приложения
├── environments/                # Переменные окружения
│   └── environment.ts          # Настройки Supabase
└── styles.scss                  # Глобальные стили
```

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js 18+ и npm
- Аккаунт Supabase

### Шаги установки

1. **Клонируйте репозиторий** (если применимо):
   ```bash
   git clone <repository-url>
   cd finance-tracker
   ```

2. **Установите зависимости**:
   ```bash
   npm install
   ```

3. **Настройте Supabase**:
   - Создайте проект на [supabase.com](https://supabase.com)
   - Создайте таблицы в базе данных (см. раздел "Настройка базы данных")
   - Обновите `src/environments/environment.ts` с вашими данными:
     ```typescript
     export const environment = {
       supabaseUrl: 'YOUR_SUPABASE_URL',
       supabaseKey: 'YOUR_SUPABASE_ANON_KEY'
     };
     ```

4. **Запустите приложение**:
   ```bash
   npm start
   ```

5. Откройте браузер и перейдите на `http://localhost:4200`

## 🗄️ Настройка базы данных

### Создание таблиц в Supabase

Выполните следующие SQL-запросы в SQL Editor вашего проекта Supabase:

#### Таблица `items` (категории)

```sql
CREATE TABLE items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  category TEXT NOT NULL CHECK (category IN ('income', 'expense')),
  total NUMERIC(10, 2) NOT NULL DEFAULT 0,
  last_updated TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Индекс для быстрого поиска
CREATE INDEX idx_items_category ON items(category);
```

#### Таблица `transactions` (транзакции)

```sql
CREATE TABLE transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  item_id UUID NOT NULL REFERENCES items(id) ON DELETE CASCADE,
  type TEXT NOT NULL CHECK (type IN ('income', 'expense')),
  amount NUMERIC(10, 2) NOT NULL,
  date TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Индексы для быстрого поиска
CREATE INDEX idx_transactions_item_id ON transactions(item_id);
CREATE INDEX idx_transactions_date ON transactions(date);
```

### Настройка Row Level Security (RLS)

Для безопасности данных настройте RLS политики:

```sql
-- Включаем RLS для таблиц
ALTER TABLE items ENABLE ROW LEVEL SECURITY;
ALTER TABLE transactions ENABLE ROW LEVEL SECURITY;

-- Политики для items (разрешаем все операции для анонимных пользователей)
-- ВНИМАНИЕ: Для продакшена настройте аутентификацию!
CREATE POLICY "Allow all operations on items" ON items
  FOR ALL USING (true) WITH CHECK (true);

-- Политики для transactions
CREATE POLICY "Allow all operations on transactions" ON transactions
  FOR ALL USING (true) WITH CHECK (true);
```

## 📚 Архитектура приложения

### Паттерны проектирования

1. **Repository Pattern** - абстракция доступа к данным
2. **Store Pattern** - управление состоянием через Angular Signals
3. **Feature-based Structure** - организация кода по функциональности

### Поток данных

```
Component → Store → Repository → Supabase
                ↓
            Signals (реактивное состояние)
```

### Основные компоненты

- **Stores** (`ItemStore`, `TransactionStore`) - управляют состоянием и бизнес-логикой
- **Repositories** (`ItemSupabaseRepository`, `TransactionSupabaseRepository`) - работают с базой данных
- **Pages** (`HomePage`, `ItemDetailsPage`) - компоненты страниц
- **Shared Components** (`ChartComponent`) - переиспользуемые компоненты

## 🎨 Стилизация

Приложение использует:
- CSS переменные для темизации
- Адаптивный дизайн (mobile-first)
- Минималистичный современный UI
- Поддержка темной темы

### Цветовая схема

- **Primary**: `#6366f1` (индиго)
- **Income**: `#10b981` (зеленый)
- **Expense**: `#ef4444` (красный)

## 📊 Графики

График транзакций поддерживает группировку по:
- **Дни** - ежедневная агрегация
- **Недели** - недельная агрегация
- **Месяцы** - месячная агрегация
- **Годы** - годовая агрегация

Переключение между периодами доступно через кнопки в интерфейсе графика.

## 🔧 Разработка

### Команды

```bash
# Запуск в режиме разработки
npm start

# Сборка для продакшена
npm run build

# Запуск тестов
npm test

# Сборка в watch режиме
npm run watch
```

### Структура кода

- Все компоненты - standalone
- Использование Angular Signals для реактивности
- TypeScript строгая типизация
- Комментарии JSDoc для документации

## 🐛 Решение проблем

### Ошибка подключения к Supabase

1. Проверьте правильность URL и ключа в `environment.ts`
2. Убедитесь, что RLS политики настроены корректно
3. Проверьте структуру таблиц в базе данных

### Ошибка 409 Conflict при сохранении

- Убедитесь, что категория (item) сохранена перед созданием транзакции
- Проверьте foreign key constraints в базе данных

### График не отображается

- Проверьте наличие транзакций для категории
- Убедитесь, что Chart.js установлен корректно

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👥 Автор

Разработано с использованием Angular и Supabase.

---

**Примечание**: Для продакшена обязательно настройте аутентификацию пользователей и более строгие RLS политики в Supabase.
