# 🚀 Быстрый старт: Деплой на GitHub Pages

> **Если у вас НЕТ кастомного домена**, используйте инструкцию: `QUICK_START_NO_DOMAIN.md`

## ✅ Что уже готово:
- ✅ GitHub Actions workflow настроен
- ✅ Production конфигурация создана
- ✅ Все изменения закоммичены

## 📋 Что нужно сделать СЕЙЧАС:

### 1. Создайте репозиторий на GitHub

1. Откройте [https://github.com/new](https://github.com/new)
2. Имя репозитория: `finance-tracker` (или любое другое)
3. **Важно**: Выберите **Private** (так как в коде есть секретные ключи)
4. НЕ добавляйте README, .gitignore или лицензию
5. Нажмите **Create repository**

### 2. Подключите локальный репозиторий

Скопируйте команды, которые GitHub покажет после создания репозитория, или выполните:

```bash
git remote add origin https://github.com/YOUR_USERNAME/finance-tracker.git
git push -u origin master
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

### 3. Настройте GitHub Pages

1. В репозитории перейдите: **Settings** → **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `gh-pages` → `/ (root)`
4. Нажмите **Save**

### 4. Дождитесь деплоя

1. Перейдите в раздел **Actions** вашего репозитория
2. Дождитесь завершения workflow "Deploy to GitHub Pages" (зеленая галочка)
3. Ваш сайт будет доступен по адресу:
   - `https://YOUR_USERNAME.github.io/finance-tracker` (стандартный домен)
   - Или ваш кастомный домен (если настроили)

### 5. Настройте ВКонтакте (важно!)

1. Откройте [https://vk.com/apps?act=manage](https://vk.com/apps?act=manage)
2. Выберите ваше приложение
3. **Доверенный redirect URI**: добавьте
   ```
   https://YOUR_USERNAME.github.io/finance-tracker/auth/callback
   ```
4. **Базовый URI**: укажите
   ```
   https://YOUR_USERNAME.github.io/finance-tracker
   ```

## ⚠️ Важные замечания:

1. **Если используете кастомный домен:**
   - Откройте `public/CNAME` и укажите ваш домен
   - Настройте DNS записи (см. `docs/GITHUB_PAGES_DEPLOY.md`)
   - Обновите redirect URI в ВКонтакте на ваш домен

2. **Если репозиторий публичный:**
   - Секретные ключи в `environment.prod.ts` будут видны всем!
   - Рекомендуется использовать GitHub Secrets (см. `docs/GITHUB_PAGES_DEPLOY.md`)

## 📚 Подробные инструкции:

- Полная инструкция: `docs/GITHUB_PAGES_DEPLOY.md`
- Настройка ВКонтакте: `docs/VK_AUTH_SETUP.md`
- Пошаговый гайд: `DEPLOY_STEPS.md`

## 🆘 Если что-то не работает:

1. Проверьте раздел **Actions** - есть ли ошибки в workflow
2. Проверьте настройки **Pages** в Settings
3. Убедитесь, что ветка называется `master` (не `main`)
4. Подождите 5-10 минут после первого деплоя

---

**Готово! После выполнения этих шагов ваш сайт будет работать на GitHub Pages! 🎉**

