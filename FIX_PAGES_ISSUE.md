# 🔧 Исправление: Вместо сайта показывается документация

## Проблема
GitHub Pages показывает README.md или другую документацию вместо вашего приложения.

## Решение

### Шаг 1: Проверьте, что деплой прошел успешно

1. Перейдите в раздел **Actions** вашего репозитория
2. Найдите последний запуск workflow "Deploy to GitHub Pages"
3. Убедитесь, что он завершился успешно (зеленая галочка ✅)
4. Если есть ошибки - исправьте их

### Шаг 2: Проверьте настройки GitHub Pages

1. Перейдите: **Settings** → **Pages**
2. Убедитесь, что настроено:
   - **Source**: `Deploy from a branch` (НЕ "Deploy from a branch" с master!)
   - **Branch**: `gh-pages` (НЕ `master` или `main`!)
   - **Folder**: `/ (root)`
3. Если выбрана ветка `master` или `main` - измените на `gh-pages`
4. Нажмите **Save**

### Шаг 3: Проверьте ветку gh-pages

1. Перейдите в раздел **Code** вашего репозитория
2. В выпадающем списке веток выберите `gh-pages`
3. Убедитесь, что там есть файлы:
   - `index.html`
   - `main-*.js`
   - `styles-*.css`
   - и другие файлы из папки `dist/finance-tracker/browser`

### Шаг 4: Если ветки gh-pages нет или она пустая

Если ветки `gh-pages` нет или она не содержит собранное приложение:

1. Проверьте логи в **Actions** - есть ли ошибки при сборке
2. Убедитесь, что workflow файл `.github/workflows/deploy.yml` находится в репозитории
3. Попробуйте запустить workflow вручную:
   - Перейдите в **Actions**
   - Выберите workflow "Deploy to GitHub Pages"
   - Нажмите **Run workflow** → **Run workflow**

### Шаг 5: Очистите кеш браузера

После исправления настроек:
1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Или откройте сайт в режиме инкогнито
3. Подождите 1-2 минуты после изменения настроек

## Частые ошибки

### ❌ Ошибка: "Source" установлен на master/main
**Решение**: Измените на `gh-pages`

### ❌ Ошибка: Ветка gh-pages не существует
**Решение**: Дождитесь успешного завершения workflow в Actions

### ❌ Ошибка: Ветка gh-pages пустая
**Решение**: Проверьте логи workflow на наличие ошибок сборки

### ❌ Ошибка: Показывается README.md
**Решение**: Это означает, что Pages показывает содержимое master/main вместо gh-pages. Измените Source на gh-pages.

## Проверка правильности настройки

После исправления ваш сайт должен:
- ✅ Открываться по адресу `https://YOUR_USERNAME.github.io/finance-tracker`
- ✅ Показывать интерфейс приложения (не README)
- ✅ Работать навигация и все функции

Если проблема не решена, проверьте:
1. Логи в разделе Actions
2. Содержимое ветки gh-pages
3. Настройки Pages в Settings

