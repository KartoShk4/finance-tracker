# 🚀 Быстрый старт: Деплой на GitHub Pages (БЕЗ кастомного домена)

## ✅ Что уже готово:
- ✅ GitHub Actions workflow настроен для стандартного GitHub Pages домена
- ✅ Production конфигурация создана
- ✅ Все изменения закоммичены

## 📋 Что нужно сделать СЕЙЧАС:

### 1. Создайте репозиторий на GitHub

1. Откройте [https://github.com/new](https://github.com/new)
2. Имя репозитория: `finance-tracker` (или любое другое - **запомните это имя!**)
3. **Важно**: Выберите **Private** (так как в коде есть секретные ключи)
4. НЕ добавляйте README, .gitignore или лицензию
5. Нажмите **Create repository**

### 2. Подключите локальный репозиторий

Скопируйте команды, которые GitHub покажет после создания репозитория, или выполните:

```bash
git remote add origin https://github.com/YOUR_USERNAME/finance-tracker.git
git push -u origin master
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

### 3. Настройте GitHub Pages

1. В репозитории перейдите: **Settings** → **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `gh-pages` → `/ (root)`
4. Нажмите **Save**

### 4. Дождитесь деплоя

1. Перейдите в раздел **Actions** вашего репозитория
2. Дождитесь завершения workflow "Deploy to GitHub Pages" (зеленая галочка)
3. Ваш сайт будет доступен по адресу:
   ```
   https://YOUR_USERNAME.github.io/finance-tracker
   ```
   **Замените:**
   - `YOUR_USERNAME` - на ваш GitHub username
   - `finance-tracker` - на имя вашего репозитория

### 5. Настройте ВКонтакте (важно!)

1. Откройте [https://vk.com/apps?act=manage](https://vk.com/apps?act=manage)
2. Выберите ваше приложение
3. **Доверенный redirect URI**: добавьте
   ```
   https://YOUR_USERNAME.github.io/finance-tracker/auth/callback
   ```
   **Замените `YOUR_USERNAME` и `finance-tracker` на ваши значения!**
4. **Базовый URI**: укажите
   ```
   https://YOUR_USERNAME.github.io/finance-tracker
   ```
   **Замените `YOUR_USERNAME` и `finance-tracker` на ваши значения!**

## 📝 Пример:

Если ваш GitHub username: `ivanov`, а репозиторий называется `finance-tracker`, то:
- Сайт будет доступен: `https://ivanov.github.io/finance-tracker`
- Redirect URI в ВКонтакте: `https://ivanov.github.io/finance-tracker/auth/callback`
- Базовый URI в ВКонтакте: `https://ivanov.github.io/finance-tracker`

## ⚠️ Важные замечания:

1. **Имя репозитория важно!** 
   - URL сайта зависит от имени репозитория
   - Если вы назвали репозиторий по-другому, используйте это имя в настройках ВКонтакте

2. **Если репозиторий публичный:**
   - Секретные ключи в `environment.prod.ts` будут видны всем!
   - Рекомендуется использовать GitHub Secrets (см. `docs/GITHUB_PAGES_DEPLOY.md`)

3. **После первого деплоя:**
   - Подождите 5-10 минут, пока GitHub обработает деплой
   - Проверьте, что сайт открывается по правильному адресу

## 🆘 Если что-то не работает:

1. **Сайт не открывается:**
   - Проверьте раздел **Actions** - есть ли ошибки в workflow
   - Убедитесь, что в настройках **Pages** выбрана ветка `gh-pages`
   - Подождите 5-10 минут после первого деплоя

2. **ВКонтакте авторизация не работает:**
   - Проверьте, что redirect URI **точно совпадает** с вашим GitHub Pages URL
   - URL должен быть: `https://YOUR_USERNAME.github.io/REPO_NAME/auth/callback`
   - Убедитесь, что используется HTTPS
   - Проверьте консоль браузера на наличие ошибок

3. **GitHub Actions не запускается:**
   - Убедитесь, что файл `.github/workflows/deploy.yml` находится в репозитории
   - Проверьте, что вы пушите в ветку `master`

## 📚 Подробные инструкции:

- Полная инструкция: `docs/GITHUB_PAGES_DEPLOY.md`
- Настройка ВКонтакте: `docs/VK_AUTH_SETUP.md`

---

**Готово! После выполнения этих шагов ваш сайт будет работать на GitHub Pages! 🎉**

Ваш сайт будет доступен по адресу: `https://YOUR_USERNAME.github.io/finance-tracker`

