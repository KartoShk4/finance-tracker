# 🔧 Создание ветки gh-pages

## Проблема
Ветка `gh-pages` не существует, потому что GitHub Actions workflow не запустился.

## Причина
Ваша основная ветка называется `main`, а в workflow была указана ветка `master`.

## ✅ Решение (уже исправлено)

Workflow обновлен для работы с веткой `main`. Теперь нужно:

### Шаг 1: Запушьте изменения

```bash
git push origin main
```

### Шаг 2: Проверьте запуск workflow

1. Перейдите на GitHub: https://github.com/KartoShk4/finance-tracker
2. Откройте раздел **Actions**
3. Должен появиться новый запуск workflow "Deploy to GitHub Pages"
4. Дождитесь его завершения (3-5 минут)

### Шаг 3: Проверьте создание ветки gh-pages

После успешного завершения workflow:

1. В репозитории перейдите в раздел **Code**
2. В выпадающем списке веток должна появиться ветка `gh-pages`
3. Выберите её и убедитесь, что там есть файлы:
   - `index.html`
   - `main-*.js`
   - `styles-*.css`

### Шаг 4: Настройте GitHub Pages

1. Перейдите: **Settings** → **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `gh-pages` → `/ (root)`
4. Нажмите **Save**

### Шаг 5: Проверьте сайт

Через 1-2 минуты после настройки Pages ваш сайт будет доступен:
```
https://KartoShk4.github.io/finance-tracker
```

## Если workflow не запустился

Если после push в разделе Actions ничего не появилось:

1. Проверьте, что файл `.github/workflows/deploy.yml` находится в репозитории
2. Убедитесь, что вы пушите в ветку `main`
3. Попробуйте запустить workflow вручную:
   - Перейдите в **Actions**
   - Выберите workflow "Deploy to GitHub Pages"
   - Нажмите **Run workflow** → выберите ветку `main` → **Run workflow**

## Если workflow завершился с ошибкой

1. Откройте запуск workflow в разделе **Actions**
2. Посмотрите, на каком шаге произошла ошибка
3. Проверьте логи ошибки
4. Частые проблемы:
   - Ошибки сборки (проверьте `npm run build` локально)
   - Отсутствие файла `environment.prod.ts`
   - Проблемы с правами доступа

## После успешного деплоя

Ветка `gh-pages` будет автоматически обновляться при каждом push в `main`.

---

**Важно**: После исправления workflow обязательно выполните `git push origin main`!

